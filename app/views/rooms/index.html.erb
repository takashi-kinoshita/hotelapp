<h1>施設一覧</h1>

<table>
  <thead>
    <tr>
      <th>施設画像</th>
      <th>施設名</th>
      <th>紹介文</th>
      <th>宿泊料金(/日)</th>
      <th>作成日</th>
    </tr>
  </thead>
  <tbody>
   <% @rooms.each do |room| %>
  <tr>
    <td><%= image_tag (room.image.blank? ? "default_room.png" : room.image), alt: "施設画像", width: "100" %></td>
    <td><%= link_to room.name, room_path(room) %></td>
    <td><%= room.description %></td>
    <td><%= room.price %>円</td>
  </tr>
<% end %>


  </tbody>
</table>

<%= link_to "施設を追加", new_room_path %>
